{
  "name": "cross-step",
  "version": "1.0.0-rc1",
  "description": "",
  "type": "module",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rome": "^11.0.0",
    "vite": "^4.0.5"
  },
  "scripts": {
    "dev": "npx vite ./src",
    "build": "npx vite build --emptyOutDir --outDir ../dist ./src",
    "clean": "rm -rf ./dist",
    "lint": "npx rome check ./src",
    "format": "npx rome format ./src"
  }
}
