{
  "name": "cross-step",
  "version": "1.0.0-rc1",
  "description": "",
  "type": "module",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rome": "^12.1.3",
    "vite": "^5.2.11"
  },
  "scripts": {
    "dev": "npx vite ./src",
    "build": "npx vite build --emptyOutDir --outDir ../dist ./src",
    "clean": "rm -rf ./dist",
    "lint": "npx rome check ./src",
    "format": "npx rome format ./src"
  }
}
